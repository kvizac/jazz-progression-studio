import { Field,Slider,Hint } from './ui';
import { changeHarmony,newProgression,chordSuggestions,applySuggestion,swapChords } from './studioActions';
import { PerformanceControls } from './PerformanceControls';
import { useEffect, useMemo, useRef, useState } from 'react';
import type { ChangeEvent, ReactNode } from 'react';
import { DEFAULT, KEYS, STYLES, INTERVALS, QUALITY_LABELS, generate, revoice, transpose, performance, freshSeed, noteName, makeChord, barCount, chordLabel, validateProject } from './studio';
import type { Project, Settings, Cell } from './studio';
import type { ChordQuality, KeyName } from './types';
import { audition, playProject, stopAudio } from './studioAudio';
import { midiBytes, download } from './studioMidi';
import { APP_VERSION } from './version';
import { PC } from './theory';

const icons:Record<string,ReactNode>={play:<path d="m8 5 11 7-11 7Z"/>,stop:<rect x="6" y="6" width="12" height="12" rx="1"/>,refresh:<><path d="M20 7v5h-5M4 17v-5h5"/><path d="M6 7a7 7 0 0 1 12-1l2 3M4 15l2 3a7 7 0 0 0 12-1"/></>,download:<><path d="M12 3v12m-5-5 5 5 5-5M4 16v5h16v-5"/></>,undo:<><path d="m8 4-5 5 5 5M3 9h10a7 7 0 0 1 7 7"/></>,redo:<><path d="m16 4 5 5-5 5m5-5H11a7 7 0 0 0-7 7"/></>,lock:<><rect x="5" y="10" width="14" height="11" rx="2"/><path d="M8 10V6a4 4 0 0 1 8 0v4"/></>,unlock:<><rect x="5" y="10" width="14" height="11" rx="2"/><path d="M8 10V6a4 4 0 0 1 8 0"/></>,note:<><path d="M9 18V5l11-2v13M9 8l11-2"/><ellipse cx="6" cy="18" rx="3" ry="2"/><ellipse cx="17" cy="16" rx="3" ry="2"/></>,save:<><path d="M4 3h13l4 4v14H3V3Z"/><path d="M7 3v6h9V3M7 21v-8h10v8"/></>,folder:<path d="M3 7h7l2-3h8v16H3Z"/>};
function Icon({name}:{name:string}){return <svg width="19" height="19" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.7" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true">{icons[name]}</svg>;}
function Toggle({label,value,onChange}:{label:string;value:boolean;onChange:()=>void}){return <button type="button" title={label==='Loop'?'Repeat the selected range continuously.':label==='Click'?'Hear a metronome during playback. The click is not included in MIDI.':'Hear four count-in beats before the progression starts.'} className={`toggle ${value?'on':''}`} aria-pressed={value} onClick={onChange}><i/>{label}</button>;}
const STORAGE='jazz-studio-v6';
function initial(){try{const saved=localStorage.getItem(STORAGE);if(saved)return validateProject(JSON.parse(saved));}catch{/* A corrupt or obsolete autosave must not block startup. */}return generate(DEFAULT);}
export default function App(){
 const [project,setProject]=useState<Project>(initial),[selected,setSelected]=useState('0:0'),[playing,setPlaying]=useState(false),[loading,setLoading]=useState(false),[beat,setBeat]=useState(-1),[status,setStatus]=useState('Ready. Select a chord to make it yours.'),[history,setHistory]=useState<Project[]>([]),[future,setFuture]=useState<Project[]>([]),[range,setRange]=useState<[number,number]>([0,barCount(project.settings)]),[exportMode,setExportMode]=useState<'performance'|'blocks'>('performance'),[tracks,setTracks]=useState<'all'|'chords'|'bass'>('all'),[zoom,setZoom]=useState(4),[help,setHelp]=useState(false),[panel,setPanel]=useState<'compose'|'perform'|'ideas'>('compose'),[changedBars,setChangedBars]=useState<number[]>([]),[busy,setBusy]=useState(false),[ideas,setIdeas]=useState<(Project|null)[]>(()=>{try{const a=JSON.parse(localStorage.getItem('studio-ideas')||'[]');return [0,1,2].map(i=>a[i]?validateProject(a[i]):null);}catch{return [null,null,null];}}),[muted,setMuted]=useState({chords:false,bass:false});
 const s=project.settings,n=barCount(s),cell=project.cells.find(c=>c.id===selected)??project.cells[0],notes=useMemo(()=>performance(project),[project]);
 const livePerformance=useRef({project,notes});
 livePerformance.current={project,notes:notes.filter(n=>!muted[n.track])};
 const performancePatch=(patch:Partial<Settings>)=>{setHistory(h=>[...h,project].slice(-50));setFuture([]);const next={...project,settings:{...s,...patch}};livePerformance.current={project:next,notes:performance(next).filter(n=>!muted[n.track])};setProject(next);setStatus(playing?'Performance updated · hear changes from the next bar.':'Performance updated. Press Play to audition.');};
 const upload=useRef<HTMLInputElement>(null),chart=useRef<HTMLCanvasElement>(null),transportToken=useRef(0);
 const stop=()=>{transportToken.current++;try{stopAudio();}catch{/* Editing remains available if audio is unavailable. */}setPlaying(false);setLoading(false);setBeat(-1);};
 const commit=(p:Project,message?:string)=>{stop();setChangedBars(Array.from({length:barCount(p.settings)},(_,b)=>b).filter(b=>JSON.stringify(project.cells.filter(c=>c.bar===b).map(c=>c.chord.symbol))!==JSON.stringify(p.cells.filter(c=>c.bar===b).map(c=>c.chord.symbol))));setHistory(h=>[...h,project].slice(-50));setFuture([]);setProject(p);if(barCount(p.settings)!==n)setRange([0,barCount(p.settings)]);if(message)setStatus(message);};
 const setting=<K extends keyof Settings>(key:K,value:Settings[K])=>{commit({...project,settings:{...s,[key]:value}});};
 const harmonic=(patch:Partial<Settings>)=>{try{commit(changeHarmony(project,patch),'Harmony updated. Locked bars kept.');}catch(e){setStatus((e as Error).message);}};
 const regenerate=(selectedRange=false)=>{if(busy)return;setBusy(true);setStatus('Writing a new progression…');setTimeout(()=>{try{const result=newProgression(project,selectedRange?range:[0,n]);if(result.changed)commit(result.project,result.message);else setStatus(result.message);}catch(e){setStatus(`Could not generate: ${(e as Error).message}`);}finally{setBusy(false);}},30);};
 const unlockAll=()=>commit({...project,cells:project.cells.map(c=>({...c,locked:false}))},'All bars unlocked. Generate can now change the whole progression.');
 const lockedBars=new Set(project.cells.filter(c=>c.locked).map(c=>c.bar)).size;
 const suggestions=useMemo(()=>chordSuggestions(project,cell),[project,cell]);
 const swap=(from:string,to:string)=>{const p=swapChords(project,from,to);if(p!==project)commit(p,'Chords swapped. Bar positions and durations kept.');else setStatus('Unlock both chords before swapping them.');};
 const saveIdea=(i:number)=>{const next=ideas.map((v,j)=>j===i?JSON.parse(JSON.stringify(project)):v);setIdeas(next);try{localStorage.setItem('studio-ideas',JSON.stringify(next));setStatus(`Idea ${'ABC'[i]} saved in this browser.`);}catch{setStatus('Idea kept for this session. Use Save project for a downloadable copy.');}};
 const changeCell=(edit:(c:Cell)=>Cell,message='Chord updated.')=>commit({...project,cells:project.cells.map(c=>c.id===cell.id?edit(c):c)},message);
 const undo=()=>{if(!history.length)return;stop();const p=history[history.length-1]!;setFuture(f=>[project,...f]);setHistory(h=>h.slice(0,-1));setProject(p);setRange([0,barCount(p.settings)]);setStatus('Undone.');};
 const redo=()=>{if(!future.length)return;stop();const p=future[0];setHistory(h=>[...h,project]);setFuture(f=>f.slice(1));setProject(p);setRange([0,barCount(p.settings)]);setStatus('Redone.');};
 const play=async()=>{if(playing||loading){stop();setStatus('Stopped.');return;}const token=++transportToken.current;setLoading(true);setStatus(s.sound==='piano'?'Loading grand piano…':'Starting…');try{const ok=await playProject(project,notes,range,setBeat,()=>{setPlaying(false);setBeat(-1);setStatus('Playback finished.');},()=>livePerformance.current.notes);if(token!==transportToken.current)return;setLoading(false);setPlaying(ok);if(ok)setStatus('Playing the same performance used by MIDI export.');}catch(e){if(token!==transportToken.current)return;setLoading(false);setStatus((e as Error).message);}};
 const hear=async(ns:number[]=cell.notes)=>{try{await audition(ns,s);}catch(e){setStatus((e as Error).message);}};
 useEffect(()=>{try{localStorage.setItem(STORAGE,JSON.stringify(project));}catch{setStatus('Browser autosave unavailable. Use Save project to keep your work.');}},[project]);
 useEffect(()=>()=>{transportToken.current++;stopAudio();},[]);
 useEffect(()=>{const key=(e:KeyboardEvent)=>{if((e.target as HTMLElement).closest('input,select,textarea,button,summary'))return;if(e.code==='Space'){e.preventDefault();void play();}if((e.ctrlKey||e.metaKey)&&e.key==='z'){e.preventDefault();if(e.shiftKey)redo();else undo();}};window.addEventListener('keydown',key);return()=>window.removeEventListener('keydown',key);});
 useEffect(()=>{const cv=chart.current;if(!cv)return;const ctx=cv.getContext('2d');if(!ctx)return;const width=1200,height=110+Math.ceil(n/4)*110;cv.width=width;cv.height=height;ctx.fillStyle='#fff';ctx.fillRect(0,0,width,height);ctx.fillStyle='#15171b';ctx.font='bold 26px sans-serif';ctx.fillText(`${s.key} ${s.mode} · ${STYLES[s.style]}`,28,42);ctx.font='16px sans-serif';ctx.fillText(`${s.bpm} BPM · 4/4 · ${project.route} · Studio v${APP_VERSION}`,28,70);for(let b=0;b<n;b++){const x=28+(b%4)*286,y=100+Math.floor(b/4)*110;ctx.strokeStyle='#aeb2b9';ctx.strokeRect(x,y,286,110);ctx.font='14px sans-serif';ctx.fillStyle='#656970';ctx.fillText(`${b+1} · ${project.cells.find(c=>c.bar===b)?.section}`,x+12,y+24);const cs=project.cells.filter(c=>c.bar===b);ctx.fillStyle='#15171b';cs.forEach(c=>{ctx.font=`bold ${cs.length>2?16:22}px sans-serif`;ctx.fillText(chordLabel(c,s),x+12+(c.beat/4)*266,y+66);});}},[project,n,s]);
 const exportMidi=()=>{try{const ns=exportMode==='performance'?notes:performance(project,'blocks');if(!ns.some(v=>(tracks==='all'||v.track===tracks)&&v.beat>=range[0]*4&&v.beat<range[1]*4)){setStatus('No notes in that track. Enable Bass or export Keys.');return;}const bytes=midiBytes(project,ns,range,tracks);download(new Uint8Array(bytes).buffer,'audio/midi',`${s.key}-${s.mode}-${s.bpm}bpm-${range[1]-range[0]}bars-${exportMode}.mid`);setStatus('MIDI exported with tempo, chord markers and exact loop length.');}catch(e){setStatus((e as Error).message);}};
 const load=async(e:ChangeEvent<HTMLInputElement>)=>{try{const file=e.target.files?.[0];if(!file)return;if(file.size>1000000)throw Error('Project file is too large.');const p=validateProject(JSON.parse(await file.text()));commit(p,'Project opened.');setRange([0,barCount(p.settings)]);setSelected(p.cells[0].id);}catch(err){setStatus((err as Error).message);}e.target.value='';};
 const quality=(root:string,q:ChordQuality)=>{const chord=makeChord(root,q);const updated={...cell,chord,bassPc:chord.rootPc,locked:false};const voiced=revoice([updated],s)[0];changeCell(()=>voiced);};
 const inversion=(up:boolean)=>{const a=[...cell.notes].sort((a,b)=>a-b);if(up)a.push(a.shift()!+12);else a.unshift(a.pop()!-12);if(a[0]<24||a[a.length-1]!>96)return;changeCell(c=>({...c,notes:a}));void hear(a);};
 const split=()=>{const first=Math.floor(cell.beats/2);if(!first)return;const second={...cell,id:`${cell.bar}:${cell.beat+first}`,beat:cell.beat+first,beats:cell.beats-first,locked:false};commit({...project,cells:project.cells.flatMap(c=>c.id===cell.id?[{...c,beats:first,locked:false},second]:[c])},'Split chord. Select either half to change it.');};
 const merge=()=>{const first={...cell,id:`${cell.bar}:0`,beat:0,beats:4,locked:false};commit({...project,cells:[...project.cells.filter(c=>c.bar!==cell.bar),first].sort((a,b)=>a.bar-b.bar||a.beat-b.beat)},'One chord across this bar.');setSelected(first.id);};
 const alternatives=useMemo(()=>{const pool:ChordQuality[]=cell.chord.quality.startsWith('min')?['min7','min9','min69','minMaj7']:cell.chord.quality.startsWith('dom')?['dom13','dom7sus','dom7b9','dom7alt']:cell.chord.quality.startsWith('maj')?['maj7','maj9','maj69','maj9#11']:['halfDim7','dim7','min7'];return pool;},[cell]);
 const displayed=notes.filter(v=>v.beat>=range[0]*4&&v.beat<range[1]*4),minPitch=Math.min(48,...displayed.map(v=>v.midi))-1,maxPitch=Math.max(77,...displayed.map(v=>v.midi))+1,rowH=12,rollHeight=(maxPitch-minPitch+1)*rowH,rollWidth=Math.max(600,(range[1]-range[0])*zoom*36);
 return <div className="studio">
  <header className="topbar"><div className="brand"><span className="brand-icon"><Icon name="note"/></span><div><strong>Jazz Progression <em>Studio</em></strong><span>COMPOSITION WORKSPACE <b>v{APP_VERSION}</b></span></div></div><nav aria-label="Project"><button title="Download this complete project, including all player settings, as a JSON file." onClick={()=>download(JSON.stringify(project,null,2),'application/json',`studio-${s.key}-${s.bpm}.json`)}><Icon name="save"/><span>Save project</span></button><button title="Open a previously saved Studio project. Your current work stays in Undo." onClick={()=>upload.current?.click()}><Icon name="folder"/><span>Open</span></button><button className={help?'active':''} title="Show the workflow guide and keyboard shortcuts." aria-label="Help" onClick={()=>setHelp(v=>!v)}>?</button></nav><input ref={upload} type="file" accept=".json" hidden onChange={load}/></header>
  <div className="transport"><div className="transport-buttons"><button className="play" title="Play or stop the selected bar range. Keyboard shortcut: Space." onClick={()=>void play()} aria-label={playing||loading?'Stop playback':'Play progression'}><Icon name={playing||loading?'stop':'play'}/>{loading?'Cancel':playing?'Stop':'Play'}</button><button className="square" title="Undo the last edit. Ctrl/⌘ Z." disabled={!history.length} onClick={undo}><Icon name="undo"/></button><button className="square" title="Redo the last undone edit. Ctrl/⌘ Shift Z." disabled={!future.length} onClick={redo}><Icon name="redo"/></button></div><div className="tempo"><input aria-label="Tempo BPM" title="Preview and exported MIDI tempo, from 40 to 300 beats per minute." type="number" min="40" max="300" value={s.bpm} onChange={e=>setting('bpm',Math.max(40,Math.min(300,+e.target.value||40)))}/><span>BPM</span></div><span className="meter">4/4</span><div className="beat-counter">{beat>=0?`${Math.floor(beat/4)+1}.${Math.floor(beat%4)+1}`:beat<-1?`IN ${beat+5}`:'1.1'}<span>BAR · BEAT</span></div><div className="transport-toggles"><Toggle label="Loop" value={s.loop} onChange={()=>setting('loop',!s.loop)}/><Toggle label="Click" value={s.click} onChange={()=>setting('click',!s.click)}/><Toggle label="Count-in" value={s.countIn} onChange={()=>setting('countIn',!s.countIn)}/></div><button className="midi-top" title="Download the selected range using the MIDI content and track choices in Export below." onClick={exportMidi}><Icon name="download"/>Export MIDI</button></div>
  {help&&<div className="help"><b>Your workflow</b><span>Choose a key and tonality → Generate → click a chord to edit it → shape the players in Perform → export to your DAW. Lock bars you want to keep. Save A/B/C ideas to compare alternatives. Drag an unlocked chord onto another to swap them, or use Move in the editor. Hover a control or tap ? for help. Space plays/stops; Ctrl/⌘ Z undoes.</span><button onClick={()=>setHelp(false)}>Close</button></div>}
  <main className="workspace">
   <aside className="sidebar" id="controls-panel">
    <div className="panel-tabs" aria-label="Workspace panels">{(['compose','perform','ideas'] as const).map(k=><button key={k} aria-pressed={panel===k} className={panel===k?'active':''} onClick={()=>setPanel(k)}>{k==='compose'?'Compose':k==='perform'?'Perform':'Ideas'}</button>)}</div>
    <div className="panel-content" hidden={panel!=='compose'}><div className="panel-heading"><span className="eyebrow">HARMONY</span><h2>Shape the progression</h2></div>
    <Field label="Harmonic language"><select value={s.style} onChange={e=>harmonic({style:e.target.value as Settings['style']})}>{Object.entries(STYLES).map(([k,v])=><option key={k} value={k}>{v}</option>)}</select></Field>
    <Field label="Form"><select value={s.form} onChange={e=>harmonic({form:e.target.value as Settings['form']})}><option value="loop">Producer phrase</option><option value="aaba">AABA · 32 bars</option><option value="blues">Jazz blues · 12 bars</option><option value="rhythm">Rhythm changes · 32 bars</option></select></Field>
    {s.form==='loop'?<div className="length-control"><span className="label">Phrase length</span><div className="segments" aria-label="Phrase length">{([4,8,16,32] as const).map(b=><button title={`Generate a ${b}-bar phrase. Locked bars within the new length stay in place.`} aria-pressed={s.bars===b} className={s.bars===b?'active':''} onClick={()=>harmonic({bars:b})} key={b}>{b}<small>bars</small></button>)}</div></div>:<p className="form-note">{n} bars · {s.form==='blues'?'call, response, turnaround':'A · A · B · A'}{s.form==='rhythm'&&s.mode==='minor'?' · minor reinterpretation':''}</p>}
    <Field label="Chord vocabulary"><select value={s.detail} onChange={e=>harmonic({detail:e.target.value as Settings['detail']})}><option value="triads">Triads</option><option value="sevenths">Sevenths</option><option value="extended">Extensions · 9 / 11 / 13</option></select></Field>
    {s.form!=='rhythm'&&s.form!=='blues'&&<Slider label="Chromatic color" value={s.color} unit="%" onChange={v=>harmonic({color:v})}/>}
    <Field label="Voicing"><select value={s.voicing} onChange={e=>{const settings={...s,voicing:e.target.value as Settings['voicing']};commit({...project,settings,cells:revoice(project.cells,settings)},'Voicings updated. Locked notes kept.');}}><option value="compact">Compact</option><option value="open">Open / drop 2</option><option value="rootless">Rootless</option></select></Field>
    <div className="lock-summary"><Icon name={lockedBars?'lock':'unlock'}/><div><b>{lockedBars?`${lockedBars} of ${n} bars locked`:'All bars can change'}</b><p>{lockedBars?'Locks preserve the whole bar during generation.':'Lock a bar directly on its card to keep it.'}</p></div>{lockedBars>0&&<button className="text-button" onClick={unlockAll}>Unlock all</button>}</div>
    </div>
    <div className="panel-content" hidden={panel!=='perform'}><div className="panel-heading"><span className="eyebrow">PLAYERS</span><h2>Build the feel</h2></div><Field label="Sound"><select value={s.sound} onChange={e=>setting('sound',e.target.value as Settings['sound'])}><option value="piano">Grand piano · sampled</option><option value="electric">Electric keys · FM</option><option value="guitar">Plucked keys · synth</option><option value="bass">Low keys · synth</option></select></Field><PerformanceControls s={s} patch={performancePatch}/></div>
    <div className="panel-content" hidden={panel!=='ideas'}><div className="panel-heading"><span className="eyebrow">SKETCHPAD</span><h2>Keep your alternatives</h2><p>Save the current progression, explore another, then compare or recall it.</p></div>{ideas.map((idea,i)=><article className="idea-card" key={i}><div className="idea-heading"><b>IDEA {'ABC'[i]}</b><button className="text-button" title={idea?'Replace this slot with the current progression.':'Save the current progression and player settings in this slot.'} onClick={()=>saveIdea(i)}>{idea?'Replace':'Save current'}</button></div>{idea?<><h3>{idea.route}</h3><p>{idea.settings.key} {idea.settings.mode} · {barCount(idea.settings)} bars · {idea.settings.bpm} BPM</p><div className="idea-chords">{idea.cells.slice(0,8).map(c=><span key={c.id}>{chordLabel(c,idea.settings)}</span>)}</div><button className="recall" title="Restore this idea, including its instruments and performance settings. Undo returns to your current work." onClick={()=>{commit(JSON.parse(JSON.stringify(idea)),`Idea ${'ABC'[i]} recalled. Press Play to hear it.`);setRange([0,barCount(idea.settings)]);}}>Recall idea {'ABC'[i]}</button></>:<p className="empty-idea">An empty slot for a progression worth keeping.</p>}</article>)}</div>
   </aside>
   <section className="arrangement">
    <div className="compose-strip"><Field label="Key"><select value={s.key} onChange={e=>commit(transpose(project,e.target.value as KeyName),'Existing chords and notes transposed.')} >{KEYS.map(k=><option key={k}>{k}</option>)}</select></Field><div className="tonality-control"><div className="field-label"><span>Tonality</span><Hint text="Switch between major and minor in every form. Locked bars preserve their existing chords."/></div><div className="segments tonality" aria-label="Tonality">{(['major','minor'] as const).map(mode=><button key={mode} aria-pressed={s.mode===mode} className={s.mode===mode?'active':''} title={`Generate ${mode} harmony while preserving locked bars.`} onClick={()=>harmonic({mode})}>{mode==='major'?'Major':'Minor'}</button>)}</div></div><div className="generate-wrap"><button className="generate" disabled={busy||lockedBars===n} title={lockedBars===n?'All bars are locked. Use Unlock all to allow new chords.':'Create a different progression in the chosen key, tonality and form. Locked bars are preserved.'} onClick={()=>regenerate()}><Icon name="refresh"/>{busy?'Generating…':'Generate new'}</button>{lockedBars===n&&<button className="text-button" onClick={unlockAll}>Unlock all bars</button>}</div></div>
    <div className="generation-status" role="status" aria-live="polite"><span className={busy?'status-pulse':''}>{status}</span></div>
    <div className="arrangement-head"><div><span className="eyebrow">CHORD SEQUENCE</span><h1>{project.route}</h1><p>{s.key} {s.mode} <i/> {STYLES[s.style]} <i/> {n} bars</p></div><button className="subtle" title="Create new harmony only inside the selected range, keeping locked bars and the rest of the arrangement." disabled={busy} onClick={()=>regenerate(true)}><Icon name="refresh"/>Vary range</button></div>
    <div className="range-strip"><span>PLAY & EXPORT</span><label>From <select title="First bar included in playback and MIDI export." aria-label="First bar" value={range[0]} onChange={e=>{stop();setRange([+e.target.value,Math.max(+e.target.value+1,range[1])]);}}>{Array.from({length:n},(_,i)=><option key={i} value={i}>{i+1}</option>)}</select></label><label>to <select title="Last included bar. MIDI always starts at bar 1 in your DAW." aria-label="Last bar" value={range[1]} onChange={e=>{stop();setRange([Math.min(range[0],+e.target.value-1),+e.target.value]);}}>{Array.from({length:n},(_,i)=><option key={i} value={i+1}>{i+1}</option>)}</select></label><button title="Use the entire progression for playback and MIDI export." className="text-button" onClick={()=>{stop();setRange([0,n]);}}>Full form</button><span className="range-count">{range[1]-range[0]} bars</span></div>
    <div className="chord-grid">{Array.from({length:n},(_,bar)=>{const cs=project.cells.filter(c=>c.bar===bar),active=beat>=bar*4&&beat<(bar+1)*4,locked=cs.some(c=>c.locked);return <div key={bar} className={`bar ${active?'playing':''} ${changedBars.includes(bar)?'changed':''} ${locked?'bar-locked':''} ${bar<range[0]||bar>=range[1]?'outside':''}`}><div className="bar-heading"><span>{String(bar+1).padStart(2,'0')}</span><b>{bar===0||project.cells.find(c=>c.bar===bar-1)?.section!==cs[0].section?cs[0].section:''}</b><button className="bar-lock" aria-label={`${locked?'Unlock':'Lock'} bar ${bar+1}`} aria-pressed={locked} title={locked?'Unlock this bar so Generate can change it.':'Keep every chord and note in this bar during generation.'} onClick={()=>commit({...project,cells:project.cells.map(c=>c.bar===bar?{...c,locked:!locked}:c)},`Bar ${bar+1} ${locked?'unlocked':'locked'}.`)}><Icon name={locked?'lock':'unlock'}/></button></div><div className="bar-chords">{cs.map(c=><button key={c.id} style={{flex:c.beats}} className={`chord ${cell.id===c.id?'selected':''}`} draggable={!c.locked} onDragStart={e=>e.dataTransfer.setData('text/plain',c.id)} onDragOver={e=>e.preventDefault()} onDrop={e=>{e.preventDefault();swap(e.dataTransfer.getData('text/plain'),c.id);}} onClick={()=>{setSelected(c.id);if(!playing&&!loading)void hear(c.notes);}} title={`${chordLabel(c,s)} · click to select and hear; drag onto another unlocked chord to swap.`} aria-label={`Bar ${bar+1}, beat ${c.beat+1}: ${chordLabel(c,s)}${c.locked?', locked':''}`}><span>{chordLabel(c,s)}</span><small>{c.chord.roman} <em>{c.beats} beats</em></small></button>)}</div><div className="bar-beats">{[0,1,2,3].map(b=><i className={Math.floor(beat)===bar*4+b?'lit':''} key={b}/>)}</div></div>;})}</div>
    <div className="mobile-controls"><button onClick={()=>{setPanel('perform');document.getElementById('controls-panel')?.scrollIntoView({behavior:'smooth'});}}>Players & settings</button></div><div className="sequence-foot"><span>{project.cells.length} chords · {lockedBars} locked bars</span><button className="text-button" title="Open the sketchpad to store and compare ideas." onClick={()=>setPanel('ideas')}>Keep as an idea</button></div>
    <a className="mobile-edit" href="#chord-editor">Edit {chordLabel(cell,s)} · notes & suggestions</a>
  <section className="roll-panel"><div className="roll-head"><div><b>Performance</b><span className="track-buttons">{(['chords','bass'] as const).map(track=><button key={track} aria-pressed={!muted[track]} className={muted[track]?'muted-track':''} title={`Click to ${muted[track]?'unmute':'mute'} ${track==='chords'?'keyboard':'bass'} preview. MIDI export uses the Tracks selection below.`} onClick={()=>setMuted(m=>({...m,[track]:!m[track]}))}><i className={track==='chords'?'keys-dot':'bass-dot'}/>{track==='chords'?'Keys':'Bass'} {muted[track]?'off':'on'}</button>)}</span></div><label>Zoom <input aria-label="Piano roll zoom" type="range" min="2" max="8" value={zoom} onChange={e=>setZoom(+e.target.value)}/></label></div><div className="roll-scroll"><div className="piano-roll" style={{width:rollWidth+44,height:rollHeight+26}}><div className="roll-ruler">{Array.from({length:range[1]-range[0]},(_,i)=><span key={i} style={{left:44+i*rollWidth/(range[1]-range[0])}}>{range[0]+i+1}</span>)}</div>{Array.from({length:maxPitch-minPitch+1},(_,i)=>{const midi=maxPitch-i,black=[1,3,6,8,10].includes(midi%12);return <div className={`pitch-row ${black?'black':''}`} style={{top:26+i*rowH,height:rowH}} key={midi}><span>{midi%12===0?noteName(midi):''}</span></div>;})}{Array.from({length:(range[1]-range[0])*4+1},(_,i)=><div key={i} className={`gridline ${i%4===0?'measure':''}`} style={{left:44+i*rollWidth/((range[1]-range[0])*4),top:26,height:rollHeight}}/>)}{displayed.map((v,i)=><button aria-label={`${noteName(v.midi)}, ${v.track}, bar ${Math.floor(v.beat/4)+1}`} title={`${noteName(v.midi)} · velocity ${Math.round(v.velocity*127)}`} className={`midi-note ${v.track} ${v.cellId===cell.id?'chosen':''}`} key={i} onClick={()=>setSelected(v.cellId)} style={{left:44+(v.beat-range[0]*4)/((range[1]-range[0])*4)*rollWidth,top:27+(maxPitch-v.midi)*rowH,width:Math.max(3,v.duration/((range[1]-range[0])*4)*rollWidth),height:rowH-2,opacity:.5+v.velocity*.5}}/>)}{beat>=range[0]*4&&beat<range[1]*4&&<div className="playhead" style={{left:44+(beat-range[0]*4)/((range[1]-range[0])*4)*rollWidth,height:rollHeight+26}}/>}</div></div><div className="roll-foot">{notes.length} notes <span>Timing, voicings and velocity match Performance MIDI.</span></div></section>
  <section className="export-panel"><div><span className="eyebrow">03 / TAKE IT TO YOUR DAW</span><h2>Keep the performance.</h2><p>Tempo, chord markers, separate tracks.<br/>The selected {range[1]-range[0]} bars start at 1.1.</p></div><div className="export-controls"><Field label="MIDI content"><select value={exportMode} onChange={e=>setExportMode(e.target.value as typeof exportMode)}><option value="performance">Performance · as heard</option><option value="blocks">Block chords · on grid</option></select></Field><Field label="Tracks"><select value={tracks} onChange={e=>setTracks(e.target.value as typeof tracks)}><option value="all">Keys + bass</option><option value="chords">Keys only</option><option value="bass">Bass only</option></select></Field><button className="export-button" onClick={exportMidi}><Icon name="download"/>Download .mid</button><button className="text-button" onClick={()=>chart.current?.toBlob(blob=>{if(blob)download(blob,'image/png','chord-chart.png');})}>Save chart PNG</button></div></section>

   </section>
  <aside className="inspector" id="chord-editor"><div className="section-title"><span>CHORD EDITOR</span><span>{cell.bar+1}.{cell.beat+1}</span></div><div className="selected-chord"><h2>{chordLabel(cell,s)}</h2><p>{cell.chord.roman} · {cell.beats} beats</p></div><div className="inspector-actions"><button onClick={()=>void hear()}><Icon name="play"/>Audition</button><button aria-pressed={cell.locked} className={cell.locked?'active':''} onClick={()=>changeCell(c=>({...c,locked:!c.locked}),cell.locked?'Unlocked.':'Locked. This bar will survive regeneration.')}><Icon name={cell.locked?'lock':'unlock'}/>{cell.locked?'Locked':'Lock'}</button></div><div className="pair"><Field label="Root"><select value={cell.chord.rootName in PC?PC[cell.chord.rootName]:cell.chord.rootPc} onChange={e=>quality(KEYS[+e.target.value],cell.chord.quality)}>{KEYS.map((k,i)=><option key={k} value={i}>{k}</option>)}</select></Field><Field label="Bass note"><select value={cell.bassPc} onChange={e=>changeCell(c=>({...c,bassPc:+e.target.value}))}>{KEYS.map((k,i)=><option key={k} value={i}>{k}</option>)}</select></Field></div><Field label="Chord quality"><select value={cell.chord.quality} onChange={e=>quality(cell.chord.rootName,e.target.value as ChordQuality)}>{Object.keys(INTERVALS).filter(q=>!['domTriad','halfDimTriad'].includes(q)).map(q=><option key={q} value={q}>{QUALITY_LABELS[q as ChordQuality]}</option>)}</select></Field><p className="label">Same root · different color</p><div className="alternatives">{alternatives.map(q=><button key={q} className={cell.chord.quality===q?'active':''} onClick={()=>quality(cell.chord.rootName,q)}>{cell.chord.rootName}{QUALITY_LABELS[q]}</button>)}</div><div className="suggestions"><div className="field-label"><span>Suggested replacements</span><Hint text="Try a dominant that leads into the following chord, or a chord from the current key. Audition first, then Apply to replace the selected chord."/></div>{suggestions.map(({chord,reason})=><div className="suggestion" key={`${chord.rootPc}:${chord.quality}`}><button className="suggestion-audition" title={`Hear ${chord.symbol} without changing the progression.`} onClick={()=>void hear(revoice([{...cell,chord,locked:false}],s)[0].notes)}><b>{chord.symbol}</b><small>{reason}</small></button><button className="suggestion-apply" title={`Replace ${chordLabel(cell,s)} with ${chord.symbol}. Undo restores it.`} onClick={()=>commit(applySuggestion(project,cell,chord),`${chord.symbol} applied.`)}>Apply</button></div>)}</div><div className="divider"/><p className="label">Voiced notes <span>{cell.notes.length}</span></p><div className="note-editor">{cell.notes.map((pitch,i)=><div key={i}><button aria-label={`Lower ${noteName(pitch)} a semitone`} disabled={pitch<=24||cell.notes.includes(pitch-1)} onClick={()=>changeCell(c=>({...c,notes:c.notes.map((v,j)=>j===i?v-1:v).sort((a,b)=>a-b)}))}>−</button><button className="note-audition" onClick={()=>void hear([pitch])}>{noteName(pitch)}</button><button aria-label={`Raise ${noteName(pitch)} a semitone`} disabled={pitch>=96||cell.notes.includes(pitch+1)} onClick={()=>changeCell(c=>({...c,notes:c.notes.map((v,j)=>j===i?v+1:v).sort((a,b)=>a-b)}))}>+</button></div>)}</div><p className="small muted">Note edits change the voicing; the chord label stays as your harmonic reference.</p><div className="pair buttons"><button onClick={()=>inversion(false)} disabled={cell.notes[cell.notes.length-1]!-12<24}>Invert ↓</button><button onClick={()=>inversion(true)} disabled={cell.notes[0]+12>96}>Invert ↑</button><button disabled={Math.min(...cell.notes)<36} onClick={()=>changeCell(c=>({...c,notes:c.notes.map(v=>v-12)}))}>Octave −</button><button disabled={Math.max(...cell.notes)>84} onClick={()=>changeCell(c=>({...c,notes:c.notes.map(v=>v+12)}))}>Octave +</button></div><div className="move-chord"><p className="label">Move chord</p><div className="pair buttons"><button title="Swap this chord with the previous unlocked chord." disabled={project.cells[0].id===cell.id||cell.locked} onClick={()=>swap(cell.id,project.cells[project.cells.findIndex(c=>c.id===cell.id)-1].id)}>Move left</button><button title="Swap this chord with the next unlocked chord." disabled={project.cells[project.cells.length-1].id===cell.id||cell.locked} onClick={()=>swap(cell.id,project.cells[project.cells.findIndex(c=>c.id===cell.id)+1].id)}>Move right</button></div></div><div className="divider"/><p className="label">Harmonic rhythm</p><div className="pair buttons"><button disabled={cell.beats<2} onClick={split}>Split chord</button><button disabled={cell.beats===4} onClick={merge}>Fill bar</button></div><button className="reset-voicing" onClick={()=>{const cells=revoice(project.cells.map(c=>c.id===cell.id?{...c,locked:false}:c),s);changeCell(()=>cells.find(c=>c.id===cell.id)!,'Voicing restored.');}}>Restore automatic voicing</button><p className="small muted">Lock after editing to preserve your notes. Regeneration keeps the whole locked bar.</p></aside></main>
  <footer><span >Session autosaves in this browser. Save project for a portable copy.</span><button className="text-button" onClick={()=>setHelp(v=>!v)}>Help & shortcuts</button></footer><div className="credits">Piano: Salamander Grand Piano by <a href="https://github.com/sfzinstruments/SalamanderGrandPiano" target="_blank" rel="noreferrer">Alexander Holm</a> · <a href="https://creativecommons.org/licenses/by/3.0/" target="_blank" rel="noreferrer">CC BY 3.0</a> · MP3 subset via Tone.js. <span>Original phrase recipes; no AI model or copied song charts.</span></div><canvas ref={chart} hidden/>
 </div>;
}
